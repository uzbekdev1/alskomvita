{
  "name": "alskom-app",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "license": "MIT",
  "scripts": {
    "install:admin": "cd admin && npm install",
    "install:api": "cd api && npm install",
    "install": "run-p install:*",
    "build:admin": "cd admin && npm run build --if-present",
    "build:api": "cd api && npm run build",
    "build": "run-p build:*",
    "start:admin": "cd admin && npm run start",
    "start:api": "cd api && npm run start",
    "start": "run-p start:*",
    "test:admin": "cd admin && npm run test --if-present",
    "test:api": "cd api && npm run test --if-present",
    "test": "run-p test:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
